apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-default-claim
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-dynamic-sc
  resources:
    requests:
      storage: 5Gi
